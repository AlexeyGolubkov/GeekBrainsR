public class ExamplesArrList {

    // ArrayList<Fruit> arrayFruits = new ArrayList<Fruit> ((Collection<? extends Fruit>) obj);

    // ArrayList<Fruit> arrayFruits = new ArrayList<Fruit> ();
    //  arrayFruits= (ArrayList<Fruit>) obj;

    //List gfg = new ArrayList(collection);
    // List<Fruit> arrayFruits=new ArrayList((Collection) obj);
    // ArrayList<ObjElement> arrayList =
    //                 new ArrayList<ObjElement>(Arrays.asList(arrayOfObjects));

    //Fruit arrayFruits = new Fruit[obj.length];
    //System.arraycopy(obj,0,arrayFruits,obj.length);
}
